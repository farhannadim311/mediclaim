/*
 * Test setup file for ZK Claim Verifier tests
 */

import { beforeAll } from 'vitest';

beforeAll(async () => {
  // Initialize any global test setup here
  console.log('Setting up ZK Claim Verifier tests...');
});
